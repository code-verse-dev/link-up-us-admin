# API base URL (used by admin app for all backend requests)
VITE_API_URL=http://localhost:3045

# Optional: Stripe publishable key for client-side (must be VITE_ to be exposed in Vite)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
